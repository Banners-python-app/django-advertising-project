apiVersion: v1
kind: ServiceAccount
metadata:
  name: banners-app-sa
  namespace: banners-main-app
  labels:
    environment: production
    app.kubernetes.io/name: banners-main-app-deployments
    app.kubernetes.io/part-of: banners-main-app
  # IF using older IRSA (OIDC), attach the AWS IAM role ARN here:
  # annotations:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::123456789012:role/DjangoS3AccessRole